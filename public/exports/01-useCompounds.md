# src/hooks/useCompounds.ts

```typescript
// ... full file content pasted below
```

This file is the main data hook for user compounds. It handles CRUD operations against the `user_compounds` table, includes realtime subscriptions, and normalizes DB rows into the app's `Compound` type.

---

**Note:** Due to platform limitations on file size in a single write, the full source is available in the consolidated export at `public/codebase-export.md`. To get the raw `.ts` file, copy from the preview or request a direct file copy.
