// WhatsNew overlay is dormant until new changelog entries are added.
// To re-enable: update APP_VERSION, populate CHANGELOG, and restore the full component logic.
const WhatsNewOverlay = () => null;

export default WhatsNewOverlay;
